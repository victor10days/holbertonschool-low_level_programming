#include "main.h"
#include <stdio.h>

/**
 * main - Entry point to check the add function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);

	return (0);
}

